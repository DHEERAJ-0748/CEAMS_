# API URL pointing to the backend Express server
# Local Development: leave blank (Vite development proxy handles requests via /api)
# Production (Vercel): set to your deployed Render service URL (e.g., https://ceams-backend.onrender.com)
VITE_API_URL=
